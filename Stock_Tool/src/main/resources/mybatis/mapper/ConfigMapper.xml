<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="top.yueshushu.learn.mapper.ConfigMapper">

    <!-- 通用查询映射结果 -->
    <resultMap id="BaseResultMap" type="top.yueshushu.learn.pojo.Config">
        <id column="id" property="id" />
        <result column="code" property="code" />
        <result column="name" property="name" />
        <result column="code_value" property="codeValue" />
        <result column="user_id" property="userId" />
        <result column="create_time" property="createTime" />
        <result column="flag" property="flag" />
    </resultMap>

    <!-- 通用查询结果列 -->
    <sql id="Base_Column_List">
        id, code, name, code_value,user_id, create_time, flag
    </sql>
    <select id="findByUid" resultMap="BaseResultMap">
        select <include refid="Base_Column_List"/>
        from config t
        <where>
            <if test="userId != null">
                and t.user_id =#{userId}
            </if>
            <if test="code != null and code != ''">
                and t.code =#{code}
            </if>
        </where>
    </select>
    <select id="getConfig" resultMap="BaseResultMap">
        select <include refid="Base_Column_List"/>
        from config t
        where  t.user_id =#{userId}
        and t.code =#{code}
        limit 1
    </select>

</mapper>
